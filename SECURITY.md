# セキュリティについて

## セキュリティ対策

このQR出退勤記録システムには、以下のセキュリティ対策が実装されています。

### 1. JWT トークン

- **署名アルゴリズム**: HS256（HMAC-SHA256）
- **有効期限**: 3分間
- **ペイロード**: デバイスID、動作タイプ、タイムスタンプ
- **検証**: サーバー側で署名を検証

### 2. レート制限

サーバーの `/record` エンドポイントにレート制限を実装:
- **制限**: 1分あたり10リクエスト
- **目的**: DDoS攻撃やブルートフォース攻撃の防止

### 3. Cookie セキュリティ

ユーザーIDを保存するCookieに以下のフラグを設定:
- **httpOnly**: JavaScriptからのアクセスを防止（XSS対策）
- **secure**: 本番環境ではHTTPSのみで送信
- **sameSite**: CSRF攻撃を防止

### 4. パスコード管理

- **保存**: サーバー側のJSONファイルで管理
- **表示**: フロントエンドではマスク表示（***）
- **用途**: JWT署名の秘密鍵として使用

### 5. ログ記録

- **有効なリクエスト**: 記録して監査証跡を残す
- **無効なリクエスト**: エラーとして記録し、不正アクセスを検出

## リスク評価

### 低リスク

1. **パスコード漏洩**
   - 影響: 不正なQRコードの生成が可能
   - 対策: ログに記録されるため、不正利用を検出可能
   - 理由: 情報を抜く機能がないため、漏洩時の影響は限定的

2. **トークンの傍受**
   - 影響: 3分間のみ有効なため、時間制限あり
   - 対策: 短い有効期限により影響を最小化

### 推奨事項

#### 本番環境

1. **HTTPS の使用**
   ```
   本番環境では必ずHTTPSを使用してください。
   Let's Encryptなどで無料のSSL証明書を取得できます。
   ```

2. **環境変数の使用**
   ```bash
   # 本番環境フラグを設定
   export NODE_ENV=production
   ```

3. **パスコードの定期変更**
   ```
   少なくとも3ヶ月に1回はパスコードを変更することを推奨します。
   ```

4. **ログファイルのアクセス制限**
   ```bash
   # ログディレクトリの権限を制限
   chmod 700 server/logs/
   ```

5. **CDNライブラリのローカル化**
   ```
   本番環境では、CDNからライブラリを読み込むのではなく、
   ローカルにダウンロードして使用することを推奨します。
   ```

#### 開発時

1. **テスト用パスコードの使用**
   ```
   本番環境とは異なるパスコードを使用してください。
   ```

2. **ログの確認**
   ```bash
   # 無効なリクエストを確認
   tail -f server/logs/invalid-*.log
   ```

## 既知の制限事項

### 1. CDN依存

- **現状**: QRCode.jsとjoseライブラリをCDNから読み込み
- **リスク**: CDNが侵害された場合、マルウェアを配信される可能性
- **対策**: 本番環境ではライブラリをローカルにダウンロードして使用

### 2. シンプルなレート制限

- **現状**: メモリベースのシンプルなレート制限
- **制限**: サーバー再起動時にリセットされる
- **改善案**: Redisなどの外部ストレージを使用した永続的なレート制限

### 3. JSONファイルベースのデータベース

- **現状**: JSONファイルでデータを管理
- **制限**: 大量のデータには適さない、同時書き込みに弱い
- **改善案**: SQLiteやPostgreSQLなどの本格的なデータベースの使用

## 脆弱性の報告

セキュリティ上の問題を発見した場合は、以下の方法で報告してください:

1. GitHubのIssuesではなく、プライベートに報告
2. 問題の詳細と再現手順を記載
3. 可能であれば修正案も提示

## セキュリティチェックリスト

本番環境にデプロイする前に、以下を確認してください:

- [ ] HTTPSを使用している
- [ ] `NODE_ENV=production` を設定している
- [ ] パスコードが強力である（8文字以上、英数字記号を含む）
- [ ] ログディレクトリのアクセス権限を制限している
- [ ] CDNライブラリをローカル化している（推奨）
- [ ] 定期的にログを確認している
- [ ] バックアップを取っている

## 更新履歴

- **2025-11-17**: 初版作成
  - レート制限の実装
  - Cookie セキュリティの強化
  - CDN使用に関する警告の追加
